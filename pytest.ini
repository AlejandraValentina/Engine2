[pytest]
addopts = -q -m "not integration and not legacy"
markers =
    integration: marks tests as integration requiring optional deps
    legacy: marks frozen/legacy tests not run by default
    smoke: marks quick smoke checks
