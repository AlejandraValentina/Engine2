[pytest]
addopts = -m "not integration and not legacy and not perf and not stress and not system" --capture=sys
markers =
    slow: long running tests
    integration: integration/performance band tests
    legacy: legacy 0D band/trend tests
    perf: performance/fast-path parity tests
    stress: adversarial stability tests
    system: end-to-end CLI tests
